# 将本网站部署到阿里云 OSS

这是一个纯静态演讲网站：不包含 API Key，也不会直接调用百炼或高德。因此可以安全部署到 OSS，用于演讲或分享链接。

## 部署前准备

1. 登录阿里云账号。
2. 如需自定义域名，在中国大陆提供服务通常需要完成该域名的 ICP 备案；仅用 OSS 默认访问地址进行内部演示则不需要自己的域名。
3. 确认本地文件夹中含有 `index.html`、`styles.css`、`app.js` 和 `assets` 文件夹。

## 方式一：OSS 静态网站托管（推荐）

1. 进入阿里云控制台，打开 **对象存储 OSS**，创建 Bucket。
2. 地域建议选离观众较近的区域，例如北京或杭州；Bucket 名称需全球唯一。
3. Bucket 权限为“公共读”时可直接让所有人访问。若仅内部演讲，可保持私有并通过授权访问；公开分享前请确认展示内容不含敏感信息。
4. 进入 Bucket 的“文件管理”，把 `ai-nutritionist-site` **文件夹内的全部文件**上传（不要只上传外层文件夹）。上传后应能看到：
   - `index.html`
   - `styles.css`
   - `app.js`
   - `assets/avatar.png` 等图片
5. 在 Bucket 的“基础设置 / 静态页面”中启用静态网站托管：默认首页填 `index.html`，默认 404 页可填 `index.html`。
6. 用 OSS 提供的静态网站访问地址打开并检查图片、动画和三个演示按钮。

## 绑定自己的域名（可选）

1. 在 OSS Bucket 的“域名管理”绑定已备案域名，例如 `nutrition.example.com`。
2. 按控制台提示在域名 DNS 中添加 CNAME 记录，解析到 OSS 分配的目标地址。
3. 建议通过阿里云 CDN 或 OSS 的 HTTPS 配置绑定 SSL 证书，并把访问地址改为 `https://`。

## 演讲时怎么使用

- 直接打开网站链接，点击右上角“进入演示模式”；也可按键盘 `P` 进入浏览器全屏。
- 网络不稳定时，可直接双击 `index.html` 离线打开（动画和内容都可运行），但建议提前用浏览器打开一次检查。
- PPT 与网页内容是一致的双保险：投影设备或网络出现问题时，切换另一份继续讲。

## 重要安全提醒

本演示网站不放入百炼 API Key、高德 Key、钉钉 Client Secret 或任何密钥。若将来要在网页中真正调用 Agent，应增加后端（函数计算 / API 网关 / 云服务器）来保管密钥；不要把密钥写进前端 HTML、CSS 或 JavaScript。
